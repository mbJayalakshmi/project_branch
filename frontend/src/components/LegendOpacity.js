import React from 'react';
import { Legend } from 'recharts';

const LegendEffectOpacity = (props) => {
  // Your custom Legend implementation, if needed
  return <Legend {...props} />;
};

export default LegendEffectOpacity;
